= Installation

== Add the Dependency

Add kramdown to your `shard.yml`:

[source,yaml]
----
dependencies:
  kramdown:
    github: aloli-crystal/kramdown
    version: "~> 2.5.2.4"
----

Then run:

[source,bash]
----
shards install
----
